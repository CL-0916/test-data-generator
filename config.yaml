# DeepSeek API 配置
deepseek:
  api_base: "https://api.deepseek.com"
  default_model: "deepseek-chat"
  temperature: 1.0
  max_tokens: 4000

# 默认生成配置
default:
  count: 10
  scenarios: ["positive", "boundary", "negative"]
  export_format: "json"

# 输出路径
output:
  dir: "./output"